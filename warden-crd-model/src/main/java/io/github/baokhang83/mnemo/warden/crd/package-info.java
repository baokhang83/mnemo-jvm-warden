/**
 * Java model of the {@code WardenPolicy} custom resource.
 *
 * <p>These types are the schema in code: the operator tooling generates the CRD from them and
 * deserializes live {@code WardenPolicy} instances into them. The real fields
 * ({@code profiles}, {@code schedule}, {@code guardrail}, {@code blackout}) arrive in W-301;
 * for now this package only stakes out the module boundary.
 */
package io.github.baokhang83.mnemo.warden.crd;
