/**
 * Java model of the {@code WardenPolicy} custom resource.
 *
 * <p>These types are the schema in code: {@code crd-generator-maven-plugin} generates the CRD
 * YAML from {@link io.github.baokhang83.mnemo.warden.crd.WardenPolicy} at build time (into
 * {@code target/classes/META-INF/fabric8/}), and the same types deserialize live {@code
 * WardenPolicy} instances once a client exists (W-302).
 */
package io.github.baokhang83.mnemo.warden.crd;
